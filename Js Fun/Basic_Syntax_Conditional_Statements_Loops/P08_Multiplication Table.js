function getTable(num){
for (let index = 1; index <= 10; index++) {
    // console.log(num+" " + "X" + " " + index + " " + "="
    // + " " + index*num);
    
    console.log(`${num} X ${index} = ${index*num}`)
}
}

getTable(5);